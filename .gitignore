/target
/.arbor

# Python
__pycache__/
*.pyc
.pytest_cache/

# macOS
.DS_Store

# Benchmark output. Regenerated by bench/*.sh on every run, and read by
# nothing — BENCH.md is where a measurement is recorded, with the methodology
# and the caveats around it. A checked-in results file is a second source of
# truth that drifts away from the first.
/bench/results/
